<!DOCTYPE html>
<html lang="es">
    <head>
        <%- include("./partials/head") %>
    </head>
    <body>
        <header>
            <%- include("./partials/navbar") %>
        </header>

        <main class="container mt-2">
            <div class="row">
                <div class="col-6">
                    <img src="<%= user.image %>" 
                         alt="Foto de perfil del usuario"
                         class="img-fluid">
                </div>
                <div class="col-6">
                    <h2><%= user.name %></h2>
                    <ul>
                        <li>Edad: <%= user.age %></li>
                        <li>Email: <%= user.email %></li>
                        <li>Teléfono: <%= user.phone %></li>
                        <li>Ciudad: <%= user.location %></li>
                    </ul>
                </div>
            </div>
        </main>
        
        <%- include("./partials/bootstrap") %>
    </body>
</html>
